export enum Roles {
  SUPERADMIN = 'superadmin',
  ADMIN = 'admin',
  HR = 'hr',
  CANDIDATE = 'candidate',
}

export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum Level {
  JUNIOR = 'junior',
  MIDDLE = 'middle',
  SENIOR = 'senior',
}

export enum Work_Format {
  ONLINE = 'online',
  OFFLINE = 'offline',
  GIBRID = 'gibrid',
}

export enum Post_Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum Post_Type {
  RESUME = 'resume',
  VACANCY = 'vacancy',
}

export enum Task_Names {
  INVITE_FRIEND = 'invite_friend',
  JOIN_CHANNEL = 'join_channel',
  PAYMENT = 'payment',
}

export enum Task_Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum User_Status {
  ACTIVE = 'active',
  BLOCKED = 'blocked',
}

export enum Channel_Group_Type {
  MAIN = 'main',
  HANDLING = 'handling',
  CLIENT = 'client',
}

export enum Channel_Group_Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
